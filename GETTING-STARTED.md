# 從零開始・第一次裝機指南（零基礎也能照做）

> 裝好之後，日常生成請翻 **[生成手冊.md](生成手冊.md)**（按目標一節一節照做）。
> **工作坊當天**：指令不用背，照 **[WORKSHOP-COMMANDS.md](WORKSHOP-COMMANDS.md)** 這張抄——複製、改空格、貼上就能用。

> **這份文件假設你完全沒寫過程式。** 每一步都寫清楚「在**哪裡**做、**打什麼**、會**看到什麼**」。
> 跟著做，最後你會得到：(1) 一個能用 `/ta` 指令問的 24 小時助教；(2) 一份你自己單元的教材；(3) 一個能給學生的網址。
> 現場有助教協助；卡關就看最後的「卡住了？」或問工作坊助教。
> **想先看做出來長什麼樣？** 開套件內的 `example-full/`（發布版附）——一門真實課程的完整實測成品（助教＋教材＋SDD 歸檔），其 README 有「輸入→一句話→產出」對照表。

---

## 目錄

- [Part 0：第一次的準備（約 30 分鐘，只需一次）](#part-0)
- [名詞快速對照](#名詞)
- [Part 1：把套件下載並打開](#part-1)
- [Part 2：放你的講義](#part-2)
- [Part 3：做你的 24 小時助教](#part-3)
- [Part 4：用指令問助教](#part-4)
- [Part 5：做你的第一份教材](#part-5)
- [Part 6：發布給學生](#part-6)
- [卡住了？](#卡住了)

---

<a id="part-0"></a>
## Part 0：第一次的準備（約 30 分鐘，只需一次）

你需要四樣東西。**沒有任何一樣需要你會寫程式**，照步驟裝就好。

### ① 一台筆電
macOS 或 Windows 都可以。

### ② 安裝編輯器 **Cursor**（推薦，內建 AI）
1. 打開瀏覽器，到 **<https://cursor.com>**。
2. 按大大的「**Download**」，它會自動判斷你的系統。
3. **Mac**：下載完打開，把 Cursor 圖示**拖進「應用程式」資料夾**。**Windows**：雙擊安裝檔，一路按「下一步」。
4. 打開 Cursor。**你會看到**一個深色的編輯器畫面——這就對了。
   > 沒有 Cursor 也行，可改用 **VS Code**（<https://code.visualstudio.com>），步驟幾乎一樣。

### ③ 安裝終端機 AI **Claude Code**（或 Codex）
- **先認識「終端機」**：終端機就是一個可以「打指令叫電腦做事」的框。在 Cursor 裡，按鍵盤左上角 <kbd>Esc</kbd> 下方的 <kbd>&#96;</kbd>（反引號）鍵、或按 <kbd>Ctrl</kbd>+<kbd>&#96;</kbd>，畫面**下方**就會跳出終端機。
- **安裝 Claude Code**：安裝方式**以官方頁為準**（<https://code.claude.com/docs>，資訊時點 2026-07）。常見做法是在終端機貼上官方提供的一行安裝指令後按 <kbd>Enter</kbd>。
- **啟動與登入**：在終端機打 `claude` 按 <kbd>Enter</kbd>。第一次會要你登入——**用你付費的 Claude 帳號登入即可，不用自備 API key**。看到歡迎訊息就成功了。
  > 用 Codex 的話，改打 `codex`，用 ChatGPT 帳號登入。
- **裝不起來？** 這一步是最容易卡的地方，工作坊現場有助教幫你；或洽學校教務處。

### ④ 註冊 **GitHub** 帳號（選用）
1. **之後想走 GitHub 發布才需要——zip 分享全程不用**，可以先跳過。
2. 要用時到 **<https://github.com/signup>**，用 email 註冊、記住帳號密碼。

> ✅ **前三樣裝好後，以後每次用都不必再裝**（④ 等要發布時再補即可）。
> 想確認都裝好了？在套件根目錄跑環境檢查（見 [SETUP.md](SETUP.md)），它會逐項回報「就緒／缺少（附安裝指引）」，缺件不會靜默：
> - **Mac**：`bash scripts/setup-check.sh`
> - **Windows**：`powershell -ExecutionPolicy Bypass -File scripts\setup-check.ps1`
> - 工作坊當天加 `--essential`（Windows 加 `-Essential`）只看必要項。
> **選裝（用到才需要，不用先裝）**：`pandoc`（`teach-context` 讀 .docx）、`poppler/pdftotext`（**Codex** 讀 PDF 用；Claude Code 內建免裝）、`manim`＋`ffmpeg`（`teach-animation` 動畫）。完整清單與安裝/驗證指令都在 [SETUP.md](SETUP.md)。

---

<a id="名詞"></a>
## 名詞快速對照（看到不懂的字回來查）

| 詞 | 白話 |
|----|------|
| 終端機 | Cursor 下方那個能打指令的框（<kbd>Ctrl</kbd>+<kbd>&#96;</kbd> 叫出） |
| repo | 一個「專案資料夾」，放在 GitHub 上就能分享 |
| clone | 把 GitHub 上的 repo「複製一份到你電腦」 |
| skill | 一段「把某件事的做法寫好」的 AI 技能，打一句話就叫用 |
| agent／助教 | 能自己讀資料、回答的 AI；本課的「24h 助教」就是 |
| `/ta` | 一個「指令」，在對話裡打 `/ta 你的問題` 就叫助教回答 |
| course-context | 放你講義的資料夾，AI 會「先看它再回答」 |
| GitHub Pages | GitHub 免費幫你把網頁放上網，得到一個網址 |

---

<a id="part-1"></a>
## Part 1：把套件下載並打開

> **第一次就走方法 A（Download ZIP，不用學 git）——這是零基礎的推薦預設。** git（方法 B）是**選用的進階做法**，之後想「一鍵更新套件」再學就好，第一次完全不需要。
> **心理準備**：後面 AI 的步驟都很平易近人（貼一句話就動）；但**蒸餾教材與生成成品會消耗 AI 用量**，所以先從小做起（見 Part 2／3）。

**方法 A（推薦預設・最簡單，不用學 git）**
1. 到這個套件的 GitHub 網頁，按綠色「**Code**」按鈕 → **Download ZIP**。
2. 到「下載」資料夾，**解壓縮**那個 zip。
3. 打開 Cursor →「**File → Open Folder…**」→ 選你剛解壓的資料夾。
4. **你會看到**左側出現一堆檔案和資料夾（`skills/`、`course-context/`…）——就對了。

**方法 B（選用進階・用 git，之後更新方便）**
> 不會 git 可**直接略過**，用方法 A 就能開始。之後想方便更新再回來學。

在終端機打（把網址換成套件的 repo 網址）：
```bash
git clone <套件的 repo 網址>
```
然後用 Cursor 開那個資料夾。

---

<a id="part-2"></a>
## Part 2：放你的講義（餵 AI「你這門課的眼鏡」）

1. 在 Cursor 左側找到 `course-context/` 資料夾。
2. 把你的講義放進去，**一個單元一個檔**。最簡單的做法：在 `course-context/` 上按右鍵 →「New File」→ 命名 `w1-你的主題.md` → 把講義重點貼進去、存檔（<kbd>Cmd</kbd>/<kbd>Ctrl</kbd>+<kbd>S</kbd>）。
3. 英文講義沒關係，貼原文即可；**數學符號保留原樣**。
   > 細節見 `course-context/README.md`。**沒放講義，AI 會請你先放，不會亂編。**
   > **懶得手動整理？** 打 `用「teach-context」這個 skill，講義在：<資料夾或檔案路徑>。`，它會**把整個資料夾分類蒸餾**：講義→`lecture-notes/`、課綱→根 `syllabus.md`、作業→`homework/`（每份一檔）、考古題→根 `exams.md`、案例→`cases/`（**有原始教材才生成**）。助教依 `AGENT.md` 的行為規則使用它們（課務據實答、作業/考試只給提示）。**要從零做整個助教**直接跳 [Part 3](#part-3)——`teach-agent` 會自動呼叫這個蒸餾流程。
   > ⚠️ **會吃 AI 用量**：蒸餾是**一檔一單位平行處理**（同時開多個 sub-agent），檔案越多、燒得越兇。**第一次先只放前三週的素材**試水溫，別一次倒整包課程；確認順了再加量。**免費／低額度方案可能中途就把 session 額度用完、跑不完**。
   > **想列「學生課前該會什麼」？** 打 `用「teach-prereq」這個 skill，課名：____，對象：____。`，它會盤點課裡假設你已會、卻不教的**外部先備知識**，產出老師清單＋學生自我檢測。

---

<a id="part-3"></a>
## Part 3：做你的 24 小時助教

1. **準備一個原始教材資料夾**：把課綱、講義、作業、考古題放進電腦上同一個資料夾（.tex/PDF/DOCX/MD 格式不拘，不用先整理）。
   > 📍 **工作坊當天的統一慣例**：這個資料夾請**放在桌面**、命名 `rawcourse`；等一下生成的助教資料夾也**指定放桌面**——大家路徑一致，卡住時助教一眼就能找到你的檔案。
   > ⚠️ **會吃 AI 用量**：`teach-agent` 會自動蒸餾整包教材、**平行開多個 sub-agent**，資料越多燒得越兇。**第一次先只放前三週的素材**跑通整條流程，別一次倒整學期；**免費／低額度方案可能中途額度用完、跑不完**。順了再逐步加量。
2. 在 Cursor 按 <kbd>Ctrl</kbd>+<kbd>&#96;</kbd> 叫出終端機，打 `claude` 啟動（第一次見 Part 0-③），在對話裡**貼上這句**（填上你的課與教材資料夾路徑）：
   ```
   用「teach-agent」這個 skill，課名：____，授課教師：____，對象：____，原始教材在：<資料夾>。
   ```
3. **你會看到** AI 一步步幫你：複製空白骨架 `agent/` 成 `<課程代碼>-ta/` → 把你指定資料夾的教材複製進其 `source-materials/` → **有檔才生成**課綱、講義、作業、考古題、先備檢測、知識圖、學生版 README、雙轉接頭（完整清單見 [生成手冊.md](生成手冊.md) §1）→ 產出 `INITIALIZATION-REPORT.md`（集中列出需要你檢查的地方）→ 最後（系統有 git 時）`git init`＋初始 commit，讓它成為可直接發布的獨立 repo（沒 git 就跳過，zip 交付不受影響）。
4. 完成後，它會告訴你助教資料夾在哪。**用 Cursor 把 `<課程代碼>-ta/` 另外開起來**（File → Open Folder…選它）。
5. **驗收**：用助教資料夾裡的 `ACCEPTANCE-CHECKLIST.md` 逐項打勾（尤其「作業題只給提示、不編造」必過）；發布給學生見其 `INSTRUCTOR-MANUAL.md` 發布節。

---

<a id="part-4"></a>
## Part 4：用指令問助教

在**助教資料夾**這個視窗裡：
- 開一個 AI 對話，直接打：
  ```
  /ta 這個概念是什麼？
  ```
  （把問題換成你要問的。）
- **用 Codex 的話**：打 `Use $<你的課程代碼>-ta-agent to answer: 你的問題`。
- 兩種工具背後是**同一個大腦 `AGENT.md`**——`.claude/`（Claude Code）和 `.codex/`（Codex）只是薄轉接頭。
- **你會看到**：
  - **觀念題** → 完整回答，並告訴你「出自講義哪一節」。
  - **作業題**（例如「幫我把答案算完」）→ **只給提示、不給答案**（這是刻意設計，避免變成代寫工具）。
  - **課程沒教的** → 它會說「超出範圍／講義未提及」，**不會亂編**。

> `/ta` 沒出現在選單？確認你打開的是**助教資料夾本身**（不是它的上層），然後重開一次 Claude Code。

---

<a id="part-5"></a>
## Part 5：做你的第一份教材

回到套件視窗（或就在助教視窗也行），在 AI 對話貼（四選一）：
```
用「teach-slides」這個 skill，單元：供需均衡，對象：大一新生。      ← 投影片
用「teach-worksheet」這個 skill，單元：供需均衡，對象：大一新生。  ← 學習單
用「teach-quiz」這個 skill，單元：供需均衡，對象：大一新生。      ← 小考＋評分規準
用「teach-sim」這個 skill，我想做一個 供需拉桿 的互動教材給 大一新生。 ← 互動模擬（網路版）
```
- **你會看到**：它會先給你一份**「迷你規格」**（這份教材要做什麼、成功長什麼樣），並**標成「待審核」**——請你把它當草稿看過。它**不會自動停下來等你回覆**，會接著把成品生成；**你看了規格或成品覺得哪裡不對，回一句叫它改就好**（SDD 精神：改規格再重跑）。（**互動模擬 `teach-sim` 例外**：走完整 SDD，先給規格三件套。）
- 生成完，它會**附一份 `使用手冊-<類型>.md`**（如 `使用手冊-slides.md`）。投影片/學習單/小考/動畫的檔放在 `output/<單元>/`；**互動模擬例外——產出是套件同層的獨立資料夾 `<單元>-sim/`**（`index.html`＋手冊＋sdd-archive，已 `git init`、可直接推 Pages）。
- **怎麼看投影片？** `slides.md` 是 Marp Markdown，純文字打開只是原始碼、看不到投影片樣子。**首選：匯出 PDF**（最通用、可攜、離線，不必裝任何擴充）——用支援 Marp 的工具或指令 `marp output/<單元>/slides.md --pdf` 匯出。**備選**：在 Cursor／VS Code 裝 **Marp 預覽擴充**直接看，或把內容貼進 **Gamma** 生成投影片。（手冊「老師端」也會寫這段。）
- **重要**：AI 給的是**草稿**，請你**親自看過學科正確性**再用。
- **進階**：想做**動畫影片**（如中央極限定理）用 `用「teach-animation」這個 skill，我想做一個 ____ 的動畫。`（需選裝 manim/ffmpeg，見 [SETUP.md](SETUP.md)）；想列**課前先備知識**用 `teach-prereq`；助教**上線後要依回饋迭代**用 `teach-refine`（回饋→裁決表→你確認→最小修改→重跑驗收，見 USER-MANUAL 第四節）。九個 skill 一覽見 [USER-MANUAL.md](USER-MANUAL.md)。

---

<a id="part-6"></a>
## Part 6：發布給學生

### 助教——方式 A（推薦）：zip 分享，完全不用 git

1. 把你的 `<課程代碼>-ta/` 資料夾**打包成 zip**——**打包前把 `source-materials/` 整個資料夾移除或排除**（那裡面是你的原始檔，可能含解答；生成的內容本身已排除解答）。一行指令（在助教資料夾的上一層執行）：
   ```bash
   zip -r 課程代碼-ta.zip 課程代碼-ta -x "課程代碼-ta/source-materials/*" -x "課程代碼-ta/.git/*"
   ```
2. 把 zip 上傳到課程平台（NTU COOL 等），學生下載解壓。
3. 學生端只要三步：**裝 VS Code＋AI extension（Claude Code 或 Codex）→ Open Folder 開啟解壓的資料夾 → 打 `/ta 你的問題`**。不需要 GitHub 帳號、不需要學 git。

### 助教——方式 B（選用/課後）：GitHub repo

> ⚠️ **先看這行再開始**：Mac 第一次用 git 推送會跳出**安裝 Xcode command line tools** 的視窗（要輸入電腦密碼、等數分鐘）；Windows 需要已裝 git。時間不夠就先用方式 A，這條課後再走。

照**產出 repo**（你的 `<課程代碼>-ta/` 資料夾）內的 `INSTRUCTOR-MANUAL.md` §12——建 GitHub repo、刪 README banner、推上去，讓學生 `git clone` 後用自己的 Claude Code 開來問。好處是之後更新方便（改了 push、學生 pull）。

### 互動教材／網頁

- **單機版（最簡單）**：產出的 `<單元>-sim/index.html` **直接把檔案傳給學生就能玩**（點兩下用瀏覽器開）——不需要發布到任何地方。
- **全班共享模式（選用）**：要「記錄、看全班結果」才需要放上 **GitHub Pages**——照套件根的 [PUBLISH-GITHUB-IO.md](PUBLISH-GITHUB-IO.md)（發布版附）逐步做，得到網址發給學生。

---

<a id="卡住了"></a>
## 卡住了？

| 狀況 | 怎麼辦 |
|------|--------|
| Claude Code 裝不起來 | 這步最容易卡，找工作坊助教或看 <https://code.claude.com/docs> |
| 打 `claude` 說「找不到指令」 | 安裝沒完成或要重開終端機；重裝一次 |
| skill 打了沒反應／答得很空 | 確認 `course-context/` 有放對應單元的講義 |
| `/ta` 指令沒出現 | 打開的要是**助教資料夾本身**；重開 Claude Code |
| 互動教材共享模式連不上 | 沒設 token 會自動離線、仍可用；設 token 用 **fine-grained、只授權那一個班級 repo 的 Contents 讀寫**（勿用全帳號 token，見 PUBLISH-GITHUB-IO Part D） |
| 想換內容重做 | **先改規格再重跑**（別直接改程式），這就是 SDD 的精神 |

> 更完整的每個功能說明 → 見 [USER-MANUAL.md](USER-MANUAL.md)。
> 貫穿原則：**AI 產草稿、你審學科正確性；所有產出都要人工審核才算完成。**
