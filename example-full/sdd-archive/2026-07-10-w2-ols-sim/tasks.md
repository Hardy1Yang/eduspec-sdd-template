## 1. 建立互動頁骨架（老師可逐項打勾驗收）

- [x] 1.1 建立單檔離線頁面 output/w2-prediction/sim-ols.html：開檔即見標題、繁中大字介面與 AI 揭露句；驗證＝離線（拔網路）用瀏覽器開啟，畫面完整、console 無錯誤、grep 無 http(s) 外部資源引用。
- [x] 1.2 畫出散布圖：內建 8 筆手搖飲「最高溫 vs 日銷量」資料點與繁中座標軸標籤正確落位；驗證＝人工比對畫面上 8 點位置與資料表一致（如 (22,180)、(35,400)）。

## 2. 互動邏輯（可調直線＋MSE＋一鍵 OLS）

- [x] 2.1 斜率／截距兩支拉桿即時移動直線並重繪殘差線段，MSE 依 (1/n)Σ(yᵢ−(a+b·xᵢ))² 即時更新；驗證＝手動設 a=0、b=10，畫面 MSE 與手算值一致。
- [x] 2.2 「一鍵 OLS」按鈕把直線設為 b=Σ(xᵢ−x̄)(yᵢ−ȳ)/Σ(xᵢ−x̄)²、a=ȳ−b·x̄，並顯示 OLS 基準 MSE 與收束句「你應該注意到…」；驗證＝按鈕後畫面顯示 slope≈17.3905、intercept≈−210.9562、MSE≈30.94，與規格 Example 一致。

## 3. 紀錄與離線退回

- [x] 3.1 每次放開拉桿／按 OLS 都以代號（無個資）寫入 localStorage，並可一鍵匯出 CSV；token 欄留空時全功能離線、不出現任何錯誤訊息；驗證＝無 token 操作數次後匯出 CSV，內容含時間戳、代號、slope、intercept、MSE，console 無錯誤。

## 4. 使用手冊與人工驗收

- [x] 4.1 產出 output/w2-prediction/使用手冊-sim.md（老師端：生成／投影／發布／看紀錄；學生端：操作與代號、token 說明）；驗證＝內容審閱涵蓋老師端與學生端兩節。
- [x] 4.2 對照規格逐條人工驗收（能跑 ≠ 正確）：親手操作每個控制項並打勾五條 Requirement 的 Scenario；驗證＝驗收紀錄寫入本 tasks 完成註記。

---

**驗收紀錄（2026-07-10，實測於 Playwright 無頭瀏覽器 + node 數值比對）**

- 1.1 ✓ 單檔開啟正常、無外部資源（grep http/cdn/src=/href= 無命中）；console 僅測試伺服器 favicon 404（非頁面資源，離線開檔不發生）。
- 1.2 ✓ 8 點與資料表一致（(22,180)…(35,400)）。
- 2.1 ✓ a=0、b=10 時畫面 MSE=987.50，與手算 7900/8 一致；殘差線段即時重繪。
- 2.2 ✓ 按「一鍵 OLS」顯示 b=17.39、a=−211.0（拉桿近似顯示）、你的 MSE=30.94＝OLS MSE=30.94，收束句常駐。修正過一個 bug：拉桿步進會把 OLS 解四捨五入（曾顯示 30.99），已改為狀態保存精確解。
- 3.1 ✓ 無 token 操作，localStorage 筆數遞增且跨重載保留；CSV 匯出含 time,nickname,action,slope,intercept,mse；無錯誤訊息。
- 4.1 ✓ 使用手冊含老師端＋學生端。
- 4.2 ✓ 五條 Requirement 的 Scenario 逐條驗過（本紀錄）。學科數值另以 Python 獨立重算比對（slope 17.390527、intercept −210.956211、minMSE 30.942806）。
