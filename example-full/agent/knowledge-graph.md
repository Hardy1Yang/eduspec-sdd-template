# Econ 5166「Data Science and Social Inquiry」 課程知識圖譜（先備依賴）

> 用有向關係 `A ← B` 表示「A 需要先懂 B」。助教用它決定「該從哪個先備概念補起」，學生用它自我檢測。
> 目前僅涵蓋已載入的 w1–w3；其餘週次載入後續填。

## 概念依賴（← 表示「需要先懂」）

```
最佳預測子＝條件期望（w2）   ← 條件期望 E[y|x]、損失／風險函數（w2）、疊代期望 LIE
損失／風險函數（w2）         ← 期望值、隨機變數（w1 複習）
OLS 作為預測子（w2）         ← 線性模型與 OLS（w1）、矩陣符號 x^T β（w1）
預測誤差三分解（w2）         ← 最佳預測子（w2）、變異數 Var(y)（w1 複習）
迴歸樹＝分段常數的 E[y|x]（w3） ← 最佳預測子＝條件期望（w2）、遞迴分割
CART 演算法（w3）            ← 迴歸樹的模型設定（w3）、貪婪法 vs 全域最佳（概念）
bias–variance trade-off（w3） ← 預測誤差三分解（w2）、期望值／變異數（w1）
交叉驗證選超參數（w3）        ← bias–variance trade-off（w3）、i.i.d. 抽樣（w1）
in-sample MSE 的樂觀偏誤（w3） ← 過度配適（w3）、風險函數（w2）
梯度下降（w1）               ← 梯度／偏微分、凸性（外部先備）
多變量常態（w1）             ← 共變異數矩陣（w1）、矩陣運算（w1 複習）
```

## 主幹學習路徑（最關鍵）

1. **預測主幹**：期望值／條件期望（w1）→ 損失與風險（w2）→ 最佳預測子＝E[y|x]（w2）→ 誤差三分解（w2）→ bias–variance 與 CV（w3）
2. **模型主幹**：矩陣符號與線性模型（w1）→ OLS 預測子（w2）→ 迴歸樹作為非線性替代（w3）

## 沒打好會連環崩盤的主幹節點（老師特別放慢處，標 ⚠）

- **⚠ 條件期望 E[y|x]**：w2「最佳預測子」、w3「樹在估什麼」全都掛在它上面；學生若把樹當成「規則清單」而不是「E[y|x] 的分段常數估計」，之後隨機森林、分類都會歪。
- **⚠ 預測誤差三分解（irreducible／approximation／estimation）**：w3 的 bias–variance trade-off、CV 選模、之後所有「模型複雜度」討論都以它為骨架。
- **⚠ 風險函數（期望損失）**：「用什麼標準比較預測子」是整門課反覆使用的評準；in-sample vs out-sample 的分野也從這裡長出來。

## 外部先備知識（課前應已具備，課內不教）

> 這門課「常提到、卻假設你以前就會」的東西（由 `teach-prereq` 盤點；詳表見 `prerequisites.md`）。
> 注意：w1 是「數學複習」週，**有複習**期望值、矩陣、分配等——下面列的是 w1 也只複習不細教、或完全沒教的 root 節點。
> 助教遇學生卡住時，**先看是不是缺了這裡的外部先備**。

```
微積分（偏微分、梯度、凸性）   → 梯度下降（w1）、OLS 閉式解推導（w1）
期望值／變異數的基本操作       → 風險函數（w2）、bias–variance（w3）
條件期望與疊代期望 LIE         → 最佳預測子（w2）
矩陣代數（轉置、反矩陣、特徵值） → 資料矩陣操作、Spectral／SVD（w1）、OLS 閉式解
機率分配（CDF/pdf、常態）      → w1 分配複習、多變量常態（w1）
i.i.d. 抽樣、母體 vs 樣本      → 抽樣假設（w1）、CV 的合理性（w3）
收斂概念（LLN/CLT、plim）      → 一致性（w1）；w1 只點名不細教
R/Python/Stata 資料操作（課綱要求） → 全部作業與期末專案
```

- **⚠ 最關鍵的外部先備：條件期望（含 LIE）**——w2 最佳預測子、w3 樹的目標函數全靠它。
- **⚠ 次關鍵：矩陣代數**——w1 的 Spectral/SVD、OLS 閉式解、之後 PCA 都用到；特徵值／特徵向量課內只複習、不從頭教。

---

*標 ⚠ 者為主幹；助教遇學生卡在下游概念時，先回頭確認這些主幹是否打好。*
*本圖由 AI 依 w1–w3 講義蒸餾草擬，學科正確性請老師審核。*
